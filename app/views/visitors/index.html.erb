<h3>Welcome</h3>
<p><%= link_to 'Users:', users_path %> <%= User.count %> registered</p>

<td><%= button_to "View Leagues", leagues_path, :class => "button small", :method => :get %></td>
<div><%= button_to "Create Club", new_league_path, :class => "button medium", :method => :get %></div>
